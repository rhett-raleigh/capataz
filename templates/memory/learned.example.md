# learned (LOCAL — copy to memory/learned.md, never commit)

Curated facts the agent should always know. One per entry, each with a why.
Keep this small — it loads every run.

<!-- examples; delete these -->
- Always confirm before opening a PR.
  Why: user wants a checkpoint until a flow is trusted.
